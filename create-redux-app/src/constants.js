/* @flow */

/**
 * The constants used throughout the app
 * @type {object}
 */
type RoutesType = {
  [key: string]: string
};

/**
 * Routing related constants i.e. the the paths
 * @type {Object}
 */
export const ROUTE_PATHS: RoutesType = {
  DEFAULT: '/',
  ABOUT: 'about',
  DASHBOARD: 'dash'
};
